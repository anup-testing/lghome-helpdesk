# Copy to .env.local to run the backend against the local Docker PostgreSQL service.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lgcare?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/lgcare?schema=public"

JWT_SECRET="local-development-only"
PORT=4100
